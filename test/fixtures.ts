export const DEFAULT_TYPE = 'file';
export const DEFAULT_SIZE_PATH = `${DEFAULT_TYPE}.size`;
export const GRAASP_ACTOR = { id: 'graasp-actor-id' };
export const MOCK_ITEM = {
  id: 'item-id',
  type: DEFAULT_TYPE,
  extra: {
    [DEFAULT_TYPE]: { size: 50 },
  },
};
